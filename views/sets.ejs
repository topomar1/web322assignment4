<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css" />

    <title>Set Collection</title>
</head>
<body>
    
        <%- include('partials/navbar', {page:'/sets'})%>
        <div class="container mx-auto">
            <div class="hero bg-base-200 rounded-lg p-6">
                <div class="hero-content text-center">
                  <div class="max-w-md">
                    <h1 class="text-5xl font-bold">Collection</h1>
                    <p class="py-6">Browse the entire collection. Filter using the following themes: </p>
                    <a class="btn btn-primary btn-sm" href="/lego/sets?theme=Technic">TECHNIC</a>
                    <a class="btn btn-primary btn-sm" href="/lego/sets?theme=Basic Set">BASIC SET</a>
                    <a class="btn btn-primary btn-sm" href="/lego/sets?theme=Books">BOOKS</a>
                  </div>
                </div>
            </div>

            <br>

            <div class="overflow-x-auto">
                <table class="table">
                  
                  <tbody>
                    <% legoSet.forEach(set => { %>
                        <tr class="even:bg-transparent odd:bg-zinc-50 rounded-lg">
                          <td>
                            <div class="flex items-center gap-3">
                                <div class="avatar">
                                    <div class="w-24 rounded">
                                      <img src="<%= set.img_url %>" alt="Set-Avatar" onerror="this.src='/images/noImage.jpg'; this.onerror=null;"/>
                                    </div>
                                  </div>
                        
                              <div>
                                <h1><%= set.name %> |<a class="btn btn-ghost btn-sm uppercase" href="/lego/sets?theme=<%= set.theme %>"><%= set.theme %></a></h1>
                              </div>
                            </div>
                          </td>
                            
                          <td>
                            <h1><strong>Year</strong>: <%= set.year %> - <strong>Parts</strong>: <%= set.num_parts %></h1>
                          </td>
                          
                          <td>
                            <a class="btn btn-ghost btn-sm" href="/lego/sets/<%= set.set_num %>">Details</a></td>
                          </td>
                        </tr>
                        <% }); %>
                  </tbody>
                 
                </table>
              </div>
    
    
        </div>
      </body>
      </html>