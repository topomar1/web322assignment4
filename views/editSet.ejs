<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css" />
    <link rel="stylesheet" href="/css/custom.css" />
    <title>Edit Set: <%= legoSet.name %></title>
</head>
<body>

    <%- include('partials/navbar', {page:''})%>
    <div class="container mx-auto">
        <div class="hero bg-base-200 rounded-lg p-6">
            <div class="hero-content text-center">
              <div class="max-w-md">
                <h1 class="text-5xl font-bold">Edit Set: <%= legoSet.name %></h1>
                <p class="py-6">Make changes to your Set. </p>
              </div>
            </div>
        </div>

        <br>


            <div class="userform" style="text-align: center;">
                <form action="/lego/editSet" method="POST">
                    
                    <label for="name" required>Name: </label>
                    <input type="text" name="name" id="name" value="<%= legoSet.name %>" required>
                <br>

                <label for="num_parts" required>Number of Parts: </label>
                <input type="number" name="num_parts" id="num_parts"  value="<%= legoSet.num_parts %>" required>
            
                <br>
        
                <label for="year">Year: </label>
                <input type="number" name="year" id="year" value="<%= legoSet.year %>" required>
        
                <br>

                <label for="img_url">Image URL: </label>
                <input type="url" name="img_url" id="img_url"  value="<%= legoSet.img_url %>" required>

                
                <br>
                <label for="set_num">Set Num: </label>
                <input type="text" name="set_num" id="set_num" value="<%= legoSet.set_num %>" readonly  required>

                
                <br>
                
                <label for="theme">Theme: </label>
                <select class="form-control" id="theme_id" name="theme_id" required>
                    <% themes.forEach(theme => { %>
                        <option <%= (legoSet.theme_id == theme.id) ? "selected" : "" %> value="<%= theme.id %>">
                            <%= theme.name %>
                        </option>
                    <% }) %>
                </select>
        
                <br>
                
                
                <button type="submit">Update Set</button>
                <a href="/lego/deleteSet/<%= legoSet.set_num %>" class="btn btn-error">Delete Set</a>
        
            </form>
        </div>
        

    </div>
    
</body>
</html>